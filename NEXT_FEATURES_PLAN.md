# DeskJarvis 下一步功能规划

> **优先级：用户体验功能**  
> **时间：2026-02-06**

---

## ✅ 已修复问题

### 1. 应用名解析问题
**问题**：用户说"打开企业微信然后控制键盘输入zhangxuzheng"时，AI把整个句子都当作应用名。

**修复**：
- 在所有planner（Claude/DeepSeek/OpenAI）中添加了**应用名解析规则**
- AI现在能识别"然后"、"接着"、"再"等连接词，只取连接词之前的部分作为应用名
- 示例：
  - "打开企业微信然后控制键盘输入zhangxuzheng" → app_name: "企业微信" ✅
  - "打开Chrome然后搜索Python" → app_name: "Chrome" ✅

### 2. 中文输入问题
**问题**：输入中文"张旭政"时不知道怎么输入。

**修复**：
- 修改了`_keyboard_type`方法，检测是否包含中文字符
- 对于包含中文的文本，使用**剪贴板+粘贴（Cmd+V）**的方式，更可靠
- 对于纯英文/数字/符号，仍使用直接`keystroke`方式（更快）

---

## 🎯 下一步功能方案（优先级：用户体验）

### Phase 1: 邮件功能（1-2周）

#### 1.1 发送邮件
**功能描述**：
- 用户说"发邮件给xxx@example.com，主题是xxx，内容是xxx"
- AI自动打开邮件客户端（Mail.app）或使用SMTP发送

**实现方案**：
```python
# 方案A：使用macOS Mail.app（推荐，用户体验好）
- 打开Mail.app
- 创建新邮件
- 填写收件人、主题、内容
- 发送

# 方案B：使用SMTP（需要配置）
- 使用smtplib库
- 需要用户在设置中配置SMTP服务器、账号、密码
- 支持Gmail、Outlook、企业邮箱等
```

**工具定义**：
```python
send_email: 发送邮件
params: {
    "to": "收件人邮箱",
    "subject": "邮件主题",
    "body": "邮件内容",
    "cc": "抄送（可选）",
    "bcc": "密送（可选）",
    "attachments": ["文件路径1", "文件路径2"]  # 可选
}
```

**实现步骤**：
1. 在`system_tools.py`中添加`_send_email`方法
2. 优先使用Mail.app（macOS），如果失败则尝试SMTP
3. 在planner中添加邮件工具描述
4. 支持附件（通过文件路径）

---

### Phase 2: 社交媒体功能（1-2周）

#### 2.1 发推特（Twitter/X）
**功能描述**：
- 用户说"发推特：今天天气真好"
- AI自动打开Twitter网页或使用API发送

**实现方案**：
```python
# 方案A：浏览器自动化（推荐，无需API密钥）
- 打开twitter.com
- 检测登录状态
- 如果未登录，请求用户登录
- 找到发推文的输入框
- 输入内容
- 点击发送

# 方案B：Twitter API（需要API密钥）
- 使用tweepy库
- 需要用户在设置中配置API密钥
- 更可靠，但需要申请API权限
```

**工具定义**：
```python
post_tweet: 发推特
params: {
    "text": "推文内容",
    "images": ["图片路径1", "图片路径2"]  # 可选
}
```

**实现步骤**：
1. 在`browser.py`中添加`_post_tweet`方法
2. 使用Playwright打开Twitter网页
3. 检测登录状态（通过cookie或页面元素）
4. 如果未登录，使用`request_login`请求用户登录
5. 找到发推文的输入框（通过选择器）
6. 输入内容并发送

---

### Phase 3: 新闻监控功能（2-3周）

#### 3.1 监控新闻
**功能描述**：
- 用户说"监控关于AI的新闻，每小时检查一次"
- AI定期检查指定关键词的新闻，发现新内容时通知用户

**实现方案**：
```python
# 方案A：RSS订阅（推荐）
- 使用feedparser库解析RSS
- 支持多个新闻源（RSS URL）
- 定期检查更新
- 发现新内容时发送通知

# 方案B：网页爬虫
- 使用Playwright爬取新闻网站
- 解析HTML获取新闻标题和链接
- 定期检查更新
- 发现新内容时发送通知

# 方案C：新闻API（需要API密钥）
- 使用NewsAPI、Google News API等
- 需要API密钥
- 更可靠，但可能有请求限制
```

**工具定义**：
```python
monitor_news: 监控新闻
params: {
    "keywords": ["关键词1", "关键词2"],  # 监控的关键词
    "sources": ["RSS URL1", "RSS URL2"],  # 新闻源（可选）
    "interval": "1小时",  # 检查间隔
    "notify": True  # 发现新内容时是否通知
}

list_news_monitors: 列出所有监控任务
params: {}

stop_news_monitor: 停止监控
params: {
    "monitor_id": "监控任务ID"
}
```

**实现步骤**：
1. 创建`agent/monitors/news_monitor.py`模块
2. 使用`agent/scheduler.py`的定时任务功能
3. 实现RSS解析和网页爬虫两种方式
4. 发现新内容时使用`send_notification`通知用户
5. 在`system_tools.py`中添加监控工具接口
6. 支持多个监控任务同时运行

---

## 📋 详细实现计划

### Week 1-2: 邮件功能

**Day 1-2: Mail.app集成**
- [ ] 实现`_send_email`方法（Mail.app方式）
- [ ] 使用AppleScript打开Mail.app并创建新邮件
- [ ] 测试基本发送功能

**Day 3-4: SMTP支持**
- [ ] 实现SMTP发送功能
- [ ] 添加邮件配置界面（设置页面）
- [ ] 支持Gmail、Outlook、企业邮箱

**Day 5-7: 附件支持**
- [ ] 支持添加附件（通过文件路径）
- [ ] 测试附件功能

**Day 8-10: 集成和测试**
- [ ] 在planner中添加邮件工具描述
- [ ] 测试各种场景（纯文本、带附件、多收件人等）
- [ ] 修复bug

---

### Week 3-4: 推特功能

**Day 1-2: 浏览器自动化**
- [ ] 实现`_post_tweet`方法
- [ ] 使用Playwright打开Twitter网页
- [ ] 检测登录状态

**Day 3-4: 登录处理**
- [ ] 集成`request_login`功能
- [ ] 处理登录后的重定向
- [ ] 保存登录状态（cookie）

**Day 5-6: 发推功能**
- [ ] 找到发推文的输入框
- [ ] 输入内容并发送
- [ ] 支持图片上传（可选）

**Day 7-10: 集成和测试**
- [ ] 在planner中添加推特工具描述
- [ ] 测试各种场景（纯文本、带图片、长文本等）
- [ ] 修复bug

---

### Week 5-7: 新闻监控功能

**Day 1-3: RSS解析**
- [ ] 实现RSS解析功能
- [ ] 使用feedparser库
- [ ] 解析新闻标题、链接、发布时间

**Day 4-6: 网页爬虫**
- [ ] 实现网页爬虫功能
- [ ] 使用Playwright爬取新闻网站
- [ ] 解析HTML获取新闻内容

**Day 7-9: 定时任务**
- [ ] 集成`agent/scheduler.py`
- [ ] 实现定期检查功能
- [ ] 发现新内容时发送通知

**Day 10-14: 集成和测试**
- [ ] 在planner中添加监控工具描述
- [ ] 实现监控任务管理（创建、列表、停止）
- [ ] 测试各种场景（多个关键词、多个新闻源等）
- [ ] 修复bug

---

## 🎨 用户体验优化

### 1. 邮件功能
- **智能识别**：用户说"发邮件"时，自动识别收件人、主题、内容
- **模板支持**：支持常用邮件模板（如"会议邀请"、"工作报告"等）
- **附件智能**：用户说"发邮件并附上桌面上的xxx文件"时，自动添加附件

### 2. 推特功能
- **智能识别**：用户说"发推特"时，自动提取推文内容
- **图片支持**：用户说"发推特并附上图片"时，自动上传图片
- **字数检查**：自动检查推文字数限制（280字符）

### 3. 新闻监控
- **智能关键词**：用户说"监控AI新闻"时，自动提取关键词
- **通知优化**：发现新内容时，发送包含标题和链接的通知
- **历史记录**：保存已监控的新闻，避免重复通知

---

## 🔧 技术实现细节

### 邮件功能
```python
# agent/executor/system_tools.py
def _send_email(self, params: Dict[str, Any]) -> Dict[str, Any]:
    """
    发送邮件
    
    Args:
        params: {
            "to": "收件人邮箱",
            "subject": "邮件主题",
            "body": "邮件内容",
            "cc": "抄送（可选）",
            "bcc": "密送（可选）",
            "attachments": ["文件路径1"]  # 可选
        }
    """
    # 方案1：使用Mail.app（macOS）
    if sys.platform == "darwin":
        # 使用AppleScript打开Mail.app并创建新邮件
        script = f'''
        tell application "Mail"
            activate
            set newMessage to make new outgoing message
            tell newMessage
                set subject to "{params['subject']}"
                set content to "{params['body']}"
                make new to recipient at end of to recipients with properties {{address:"{params['to']}"}}
            end tell
            open newMessage
        end tell
        '''
        subprocess.run(["osascript", "-e", script], check=True)
    
    # 方案2：使用SMTP（跨平台）
    # ...
```

### 推特功能
```python
# agent/executor/browser.py
def _post_tweet(self, params: Dict[str, Any]) -> Dict[str, Any]:
    """
    发推特
    
    Args:
        params: {
            "text": "推文内容",
            "images": ["图片路径1"]  # 可选
        }
    """
    # 1. 打开Twitter网页
    self.page.goto("https://twitter.com")
    
    # 2. 检测登录状态
    if not self._is_logged_in():
        # 请求用户登录
        self._request_login("Twitter", ...)
    
    # 3. 找到发推文的输入框
    tweet_input = self.page.locator('[data-testid="tweetTextarea_0"]')
    tweet_input.fill(params["text"])
    
    # 4. 如果有图片，上传图片
    if params.get("images"):
        # 上传图片逻辑
        pass
    
    # 5. 点击发送按钮
    send_button = self.page.locator('[data-testid="tweetButton"]')
    send_button.click()
    
    return {"success": True, "message": "推文已发送"}
```

### 新闻监控
```python
# agent/monitors/news_monitor.py
class NewsMonitor:
    """
    新闻监控器
    """
    def __init__(self):
        self.monitors = {}  # monitor_id -> MonitorTask
    
    def create_monitor(self, keywords: List[str], sources: List[str], interval: str):
        """创建监控任务"""
        monitor_id = str(uuid.uuid4())
        task = MonitorTask(
            monitor_id=monitor_id,
            keywords=keywords,
            sources=sources,
            interval=interval
        )
        self.monitors[monitor_id] = task
        
        # 注册定时任务
        scheduler = get_scheduler()
        scheduler.add_recurring_task(
            task_id=monitor_id,
            callback=self._check_news,
            interval_seconds=parse_interval(interval),
            args=(monitor_id,)
        )
        
        return {"success": True, "monitor_id": monitor_id}
    
    def _check_news(self, monitor_id: str):
        """检查新闻更新"""
        task = self.monitors[monitor_id]
        
        # 1. 获取新闻源的最新内容
        new_items = []
        for source in task.sources:
            items = self._fetch_news(source, task.keywords)
            new_items.extend(items)
        
        # 2. 过滤已监控的内容
        new_items = [item for item in new_items if item.id not in task.seen_ids]
        
        # 3. 发送通知
        if new_items:
            for item in new_items:
                send_notification(
                    title="发现新新闻",
                    message=f"{item.title}\n{item.url}"
                )
                task.seen_ids.add(item.id)
```

---

## 📊 优先级排序

1. **邮件功能** ⭐⭐⭐⭐⭐
   - 用户需求高
   - 实现相对简单
   - 用户体验好

2. **推特功能** ⭐⭐⭐⭐
   - 用户需求中等
   - 实现中等复杂度
   - 需要处理登录

3. **新闻监控** ⭐⭐⭐
   - 用户需求中等
   - 实现较复杂
   - 需要定时任务支持

---

## 🚀 快速开始

### 第一步：邮件功能
1. 在`system_tools.py`中添加`_send_email`方法
2. 在planner中添加邮件工具描述
3. 测试基本发送功能

### 第二步：推特功能
1. 在`browser.py`中添加`_post_tweet`方法
2. 在planner中添加推特工具描述
3. 测试发推功能

### 第三步：新闻监控
1. 创建`agent/monitors/news_monitor.py`模块
2. 集成定时任务功能
3. 测试监控功能

---

## 📝 注意事项

1. **安全性**：
   - 邮件和推特的账号密码不要硬编码
   - 使用设置页面让用户配置
   - 敏感信息加密存储

2. **错误处理**：
   - 网络错误时重试
   - 登录失败时提示用户
   - API限制时等待并重试

3. **用户体验**：
   - 操作前确认（危险操作）
   - 实时反馈（发送中、已发送）
   - 错误提示清晰

---

**下一步行动**：从邮件功能开始，逐步实现其他功能。
